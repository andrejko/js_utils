module.exports = {
	removeUnpaired: function(dir, mainExt = 'jpg', pairExt = 'dng', recursive = false) {
		let processedFiles = [];

		console.log(dir, mainExt, pairExt, recursive);

		return processedFiles;
	}
}